---
name: official-document-assistant
display_name: 公文合规审查与督办助手
description: 面向办公室、行政、人事、学生组织和基层单位的正式材料处理 Skill，支持通知、请示、报告、函、会议纪要的合规审查、草稿起草、会议督办台账提取、敏感信息脱敏和版本变更说明。两层架构（智能体判断 + 确定性校验），零依赖、零联网、对异常输入不崩溃，可在 AstronClaw 部署调用。
version: 0.8.0
---

# 公文合规审查与督办助手

> 面向办公室正式材料流转场景，自动完成公文/通知/请示/报告/函/会议纪要的格式审查、风险提示、草稿起草、会议待办提取、敏感信息脱敏与版本变更说明。

办公室材料常见问题不是“不会写”，而是标题、主送机关、落款、日期、附件、请示报告混用、敏感信息、会议待办无人跟踪等细节导致反复退回。本 Skill 用“宿主大模型 + 确定性校验脚本”完成正式材料初审、修订建议、草稿生成和督办事项提取，把反复退回的痛点前置解决。

## 能力分工（重要）

本 Skill 刻意分成两层，各做自己擅长的事：

- **智能体层（由宿主大模型承担）**：负责语义理解、文种判断复核、修订稿润色、起草内容生成，以及对偏题或异常输入的友好提示与引导。
- **确定性校验层（本 Skill 附带的脚本）**：纯 Python 标准库实现，**不调用任何模型**，对格式要素、文种规则和敏感信息做可复现、不幻觉的检查与本地脱敏，并输出结构化结果。

这样分工的目的，是把“判断和改写”交给模型、把“合规校验和脱敏”交给确定性程序：避免模型在格式要素、附件编号、敏感信息这类需要精确的地方幻觉，同时让每一条审查意见都能追溯到 GB/T 9704—2012《党政机关公文格式》与《党政机关公文处理工作条例》。

## 核心模式

本 Skill 提供 5 个可组合的核心模式，覆盖办公室正式材料流转的完整闭环：

| 模式 | 用途 | 典型输入 |
| --- | --- | --- |
| `review` | 公文格式与合规审查，输出三层报告（领导摘要 / 承办人修改清单 / 建议修订稿） | 通知、请示、报告、函、会议纪要正文 |
| `draft` | 根据一句话或 JSON 需求生成公文草稿骨架，并自动跑一遍合规自检 | "帮我写一份关于……的通知" |
| `brief` | 会议纪要/简报督办台账提取，支持 Markdown 表格与 CSV 导出 | 会议纪要正文 |
| `compare` | 两版材料版本变更说明，对比标题/主送机关/日期/风险变化 | `--old old.md --new new.md` |
| `batch` | 批量材料包体检，输出风险排行表与跳过清单 | `--batch examples/review` |

## 赛题能力对齐（办公协同·skill 开发赛道）

| 评分维度 | 本 Skill 的对应设计 |
| --- | --- |
| 运行稳定性与鲁棒性 | 零依赖纯标准库、零联网；对空输入、损坏 docx、二进制、非 UTF-8、超长文本、缺失目录等异常输入做分诊与降级，**不崩溃、无 traceback**；批量审查单文件出错不影响整体。内置 30+ 异常/边界/脏样本测试用例。 |
| 创新性与应用价值 | 办公协同两大痛点直击：①对照国标的**确定性、可解释、不幻觉**格式合规校验；②个人信息/涉密**本地脱敏、数据不出域**；③新增版本变更说明，覆盖办公流转协同。区别于“纯大模型生成公文”。 |
| 结果质量 | 审查、起草、督办、对比四模式输出完整结构化结果；每条问题附国标依据；建议修订稿可直接复制再人工复核；会议督办输出可贴入 Excel 的台账表。 |
| 技术设计与场景编排 | 两层架构（智能体判断 + 确定性校验工具）；五模式可作为可组合工具被编排；附领域知识库与依据映射供模型查阅。 |
| 工程规范与文档 | 标准 SKILL.md frontmatter、完整 README、清晰目录与注释、可复现示例与输出、独立 docs/ 与 tests/ 目录。 |
| 安全合规 | 不联网、不外传、本地处理；不编造政策依据；个人信息/涉密给出脱敏与本地处理建议；独立 `docs/security.md` 与 `references/safety-policy.md`。 |

## 在 AstronClaw 上部署调用

本 Skill 为标准 Agent Skill 包：`SKILL.md`（frontmatter 含 `name`）+ `scripts/` + `references/` + `examples/` + `docs/` + `tests/`。智能体加载后，按用户意图调用 `scripts/gov_doc_review.py`（通过命令行，读取 stdin 或文件），脚本仅依赖 Python 标准库，无需安装任何第三方包，也不发起网络请求，便于在 AstronClaw 等环境稳定部署运行。

## 知识库与参考

`references/` 目录是供智能体层查阅的领域知识库：

- `references/gongwen-checklist.md`：公文主体要素清单、文种适用范围与易错点（对照 GB/T 9704—2012 与条例）。
- `references/drafting-guide.md`：分文种起草规范与异常输入引导话术，指导模型在脚本骨架上填入真实、可执行内容并避免套话。
- `references/basis-map.json`：问题类型到国标/条例依据的结构化映射，保证输出依据统一、可追溯。
- `references/safety-policy.md`：安全合规策略，明确不联网、本地脱敏、人工复核边界。

组织可在该目录追加本单位行文规则，扩展为内部知识库。

## 适用场景

当用户需要处理以下材料时使用本 Skill：

- 通知、请示、报告、函、通报等正式公文类材料。
- 会议纪要、工作简报、活动通知、学生组织通知、办公室材料。
- 需要检查标题、主送机关、正文层级、附件说明、发文机关署名、成文日期和用语规范的文本。
- 需要快速生成通知、请示、报告、函、会议纪要等可编辑草稿的文本需求。
- 需要从会议材料中提取责任单位、截止时间、待办事项，生成督办台账。
- 需要对比两版材料，输出版本变更说明与风险变化。
- 需要对手机号、身份证号、涉密或内部资料表述进行风险提示和本地脱敏预览。

## 工作流程

1. 智能体先判断材料类型：公文审查、模板起草、会议纪要/督办提取或版本对比；若输入明显偏题或信息缺失，先向用户澄清而不是硬套模板。
2. **异常输入分诊**：脚本对空输入、起草请求误投到审查模式、过短或非公文文本等情况，返回“# 输入提示”而非错误报告；遇到损坏文件、不存在路径等，返回友好提示并安全退出。智能体据此引导用户。
3. 优先读取用户直接提供的文本；如用户给出文件路径，可读取 UTF-8 文本、Markdown、JSON 或 `.docx` 文件。
4. 调用确定性校验层脚本完成格式和合规检查、敏感信息脱敏、督办事项抽取，得到结构化结果。
5. 起草模式下，根据文种、主题、主送机关、署名单位、截止时间和要求生成可编辑草稿，模型可在脚本骨架上润色，但不得编造未提供的事实；草稿生成后自动跑一遍 review 自检，输出“草稿 + 信息缺口 + 合规自检报告”。
6. 审查模式下脚本执行的检查项（均对照标准）：
   - 标题是否符合“关于 + 事由 + 文种”或“发文机关 + 关于 + 事由 + 文种”结构。
   - 是否具备主送机关、正文、发文机关署名和成文日期。
   - 成文日期是否用阿拉伯数字标注、月日不编虚位（不使用“-”“/”分隔）。
   - 附件说明与正文附件顺序号是否一致。
   - 正文层次序数是否按“一、（一）1.（1）”组织、有无跳级。
   - 请示是否一文一事、结语是否规范。
   - 报告是否夹带请示/申请事项。
   - 函的语气是否适合不相隶属机关商洽。
   - 通知是否明确对象、事项、时间节点和办理要求。
   - 是否存在口语化、强制性表述缺少依据、个人敏感信息、涉密或内部资料风险。
7. 对会议纪要和简报提取督办事项：责任方、任务描述、截止时间、原文依据，并输出可贴入 Excel 的督办台账表。
8. 输出审查报告，采用三层结构：领导摘要（一句话结论 + 三大问题）→ 承办人修改清单（逐条问题/证据/建议/依据）→ 建议修订稿；附督办台账与脱敏预览。

## 可用脚本

本 Skill 附带一个纯 Python 标准库脚本（确定性校验层）：

```bash
# 审查
python scripts/gov_doc_review.py --mode review --format markdown < examples/review/notice.md
python scripts/gov_doc_review.py --mode review --format markdown --input examples/review/notice.md
python scripts/gov_doc_review.py --mode review --format json     --batch examples/review

# 起草（自动跑合规自检）
python scripts/gov_doc_review.py --mode draft  --format markdown < examples/draft/request.txt

# 会议督办台账（支持 CSV 导出）
python scripts/gov_doc_review.py --mode brief  --format markdown < examples/brief/meeting.md
python scripts/gov_doc_review.py --mode brief  --format csv      < examples/brief/meeting.md

# 版本变更说明
python scripts/gov_doc_review.py --mode compare --old old.md --new new.md --format markdown

# docx
python scripts/gov_doc_review.py --mode review --format markdown --input demo.docx --output report.md
```

参数说明：

- `--mode review`：公文格式和合规审查。
- `--mode draft`：根据 JSON 或自然语言需求生成公文草稿骨架，并自动跑合规自检。
- `--mode brief`：会议纪要/简报督办台账提取。
- `--mode compare`：两版材料版本变更说明，需配合 `--old` 与 `--new`。
- `--format markdown`：输出适合直接复制的报告。
- `--format json`：输出结构化结果（含每条问题的 `basis` 依据字段），便于二次处理。
- `--format csv`：仅在 `brief` 模式下生效，输出可贴入 Excel 的督办台账 CSV。
- `--input`：读取单个 UTF-8 文本、Markdown、JSON 或 `.docx` 文件。
- `--old` / `--new`：`compare` 模式下指定两版材料文件路径。
- `--batch`：批量审查文件夹内的 `.txt`、`.md`、`.json`、`.docx` 文件；会自动跳过本工具生成的审查报告和起草需求 JSON，避免误审；输出材料包体检报告与风险排行。批量请指向只含待审文档的目录（如 `examples/review`）。
- `--output`：将结果写入指定文件。

## 输出要求

审查模式输出三层结构：

1. **领导摘要**：一句话风险结论 + 风险等级 + 三大主要问题。
2. **承办人修改清单**：表格形式，含序号、问题、严重程度、原文证据、修改建议、依据。
3. **建议修订稿**：生成一版可复制、可人工复核的规范文本。
4. **督办事项**：责任方、期限、事项（表格形式）。
5. **脱敏预览**：识别手机号、身份证号等个人敏感信息时输出本地脱敏预览。

起草模式输出“可编辑草稿 + 起草信息缺口 + 自动合规自检报告”。

brief 模式输出督办台账表（Markdown 表格或 CSV）。

compare 模式输出“核心变化 + 风险变化表 + 提交建议”。

## 安全与合规

- 不编造政策依据、法规名称或不存在的上级文件。问题清单中引用的依据指向 GB/T 9704—2012 与《党政机关公文处理工作条例》的**要素名称和原则**；具体条、款、项序号请以现行标准与条例正式文本为准，提交前人工核对。
- 涉及涉密、个人隐私或未公开政务数据时，只给出本地处理和脱敏建议，不建议上传外部平台。
- 输出结果仅作为材料初审和起草辅助，最终应由人工复核。
- 详细安全策略见 `docs/security.md` 与 `references/safety-policy.md`。

## 已知限制

- `.docx` 解析提取正文段落与表格文本；页眉、页脚、版记不纳入自动审查；检测到批注或修订痕迹（`w:ins`/`w:del`/`comments.xml`）时会提示用户提交前确认，但不解析具体修订内容。
- 本 Skill 聚焦公文格式校验、起草、修订建议、督办提取和版本对比，不承诺政策问答、政策依据核验或全量政务知识检索。
- 校验层为规则启发式，对“脏”输入会有少量误报/漏报，应由智能体层和人工复核兜底。
